version: 2

models:
  - name: fct_order_items
    columns:
      - name: order_id
        tests:
          - not_null

      - name: product_id
        tests:
          - relationships:
              arguments:
                to: ref('dim_products')
                field: product_id
